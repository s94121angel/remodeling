apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "charthome.fullname" . }}-test-connection"
  labels:
    {{- include "charthome.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "charthome.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
